{
  "name": "@midnight/contract",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Add contract build command\"",
    "test": "echo \"Add contract test command\"",
    "clean": "rm -rf artifacts dist"
  }
}
